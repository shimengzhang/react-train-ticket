import React, {
  PureComponent, useState, useRef, useCallback,
} from 'react';

class Counter extends PureComponent {
  render() {
    return (
      <div>
        Counter
      </div>
    );
  }
}

export default Counter;
